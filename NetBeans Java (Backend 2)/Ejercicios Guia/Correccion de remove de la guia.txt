LISTAS (Eliminar listas).

//Elimina por indice:

ArrayList<Integer> numeroA = new ArrayList<>();
int x = 20;
numeroA.add(X); //Este numero se encuentra en el indice 0.
numeroA.remove(0); //Eliminamos el numero que esta en el indice 0.

//Elimina por elemento:

ArrayList<Integer> numeroB = new ArrayList<>();
int y = 30;
numeroB.add(y);
numeroB.remove(Integer.valueOf(30)); // De esta manera eliminamos el primer numero 30 						 // que encuentre en los elementos 
